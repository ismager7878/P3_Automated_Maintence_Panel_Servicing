### 3rd semeter project
This is gonna be fire!!!!

## Using the python venv
First time initialize a venv like so:
~~~
python3 -m venv .venv
~~~
Before building activate the venv like so: 
~~~
source .venv/bin/activate
~~~
If new requirements needs to be installed use:
~~~
pip install -r requirements.txt
~~~